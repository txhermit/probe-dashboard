name=probe-dashboard
description=Live table of Wi-Fi probe requests in the Kismet WebUI
author=John Baker
version=2.0.0

# Path is relative (no leading '/'). Kismet maps /plugin/<dirname>/ to httpd/.
js=probe_dashboard,plugin/probe-dashboard/js/probe_dashboard.js
